type: object
properties:
  outboundId:
    type: integer
    description: 出库记录ID
  format:
    type: string
    description: 报告格式
    enum:
      - pdf
      - excel
    default: pdf
required:
  - outboundId
