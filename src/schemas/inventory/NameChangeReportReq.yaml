type: object
properties:
  nameChangeId:
    type: integer
    description: 名义变更记录ID
  format:
    type: string
    description: 报告格式
    enum:
      - pdf
      - excel
    default: pdf
required:
  - nameChangeId
