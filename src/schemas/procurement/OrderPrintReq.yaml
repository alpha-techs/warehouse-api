type: object
properties:
  orderId:
    type: integer
    description: 需要生成打印版的注文ID
  format:
    type: string
    description: 打印文件格式
    enum:
      - excel
    default: excel
additionalProperties: false
required:
  - orderId
