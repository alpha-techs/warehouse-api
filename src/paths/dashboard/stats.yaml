get:
  summary: 获取仪表盘统计数据
  operationId: getDashboardStats
  tags:
    - 仪表盘
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: '../../index.yaml#/components/schemas/DashboardStatsResp'
