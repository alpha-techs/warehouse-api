get:
  summary: 出库商品列表
  operationId: listOutboundItems
  tags:
    - 库存
  parameters:
    - name: page
      in: query
      description: 页码
      schema:
        type: integer
        default: 1
    - name: itemsPerPage
      in: query
      description: 每页数量
      schema:
        type: integer
        default: 20
    - name: lotNumber
      in: query
      description: 批次号
      schema:
        type: string
    - name: productId
      in: query
      description: 商品ID
      schema:
        type: integer
    - name: outboundDateFrom
      in: query
      description: 出库日期(From)
      schema:
        type: string
        format: date
    - name: outboundDateTo
      in: query
      description: 出库日期(To)
      schema:
        type: string
        format: date
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: '../../index.yaml#/components/schemas/ListOutboundItemsResp'
