post:
  summary: 取消发票
  operationId: cancelInvoice
  tags:
    - 结算
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
        format: int32
        description: 发票ID
  requestBody:
    required: false
    content:
      application/json:
        schema:
          $ref: '../../index.yaml#/components/schemas/CancelInvoiceReq'
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: '../../index.yaml#/components/schemas/Invoice'
